<?php
$escape = $this->plugin('escapeHtml');
$this->htmlElement('body')->appendAttribute('class', 'reference browse list');
// TODO Currently, the items are forced in the controller for this action.
$resourceName = 'items';
?>
<div id="primary" class="reference">
    <h2><?php echo sprintf($this->translate('Browse Items By "%s" (%d Headings)'), $slugData['label'], count($references)); ?></h2>
    <?php if (count($references)): ?>
    <?php
        echo $this->reference()->displayList($references, [
            'slug' => $slug,
            'resource_name' => $resourceName,
        ]);
    ?>
    <?php else: ?>
    <p><?php echo sprintf($this->translate('There is no references for "%s".'), $slugData['label']); ?></p>
    <?php endif; ?>
</div>
