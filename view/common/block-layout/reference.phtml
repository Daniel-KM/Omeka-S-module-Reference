<?php
$translate = $this->plugin('translate');
?>
<div class="reference-block">
    <?php if ($options['heading']): ?>
    <h2><?php echo $translate($options['heading']); ?></h2>
    <?php endif; ?>

    <?php
    if ($total):
        switch ($mode):
            case 'tree':
                $references = $args['tree'];
                unset($args['tree']);
                echo $this->reference()->displayTree($references, $args, $options);
                break;
            case 'list':
                $term = $term = $args['term'];
                echo $this->reference()->displayListForTerm($term, $args, $options);
                break;
        endswitch;
    else:
    ?>
    <p><?php echo $translate('There is no references.'); ?></p>
    <?php
    endif;
    ?>
</div>
